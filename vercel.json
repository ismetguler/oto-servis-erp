{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma migrate deploy && prisma generate && tsx prisma/arac-model-seed.ts && next build",
  "crons": [
    { "path": "/api/bakim/log-mail", "schedule": "0 4 * * 1" }
  ]
}
